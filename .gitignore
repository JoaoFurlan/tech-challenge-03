# Course material — copyrighted, not part of the submission
*.pdf
!MLET - Tech Challenge Fase 3.pdf

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.*

# uv
.python-version

# Data & model artifacts
# (models/ itself isn't ignored -- the DVC .dvc pointer files for tracked
# artifacts live inside it and need to be git-tracked; the binary patterns
# below still block the actual artifacts from ever being committed)
*.csv
data/
*.onnx
*.pkl
*.joblib

# DVC
/data
*.dvc.lock

# MLflow
mlflow.db
mlruns/
mlartifacts/

# Airflow
airflow.db
airflow-webserver.pid
logs/

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/
